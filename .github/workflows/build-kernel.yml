name: Build Kernel

on:
  workflow_dispatch:
    inputs:
      defconfig:
        description: "Defconfig target (must exist in arch/arm64/configs/)"
        required: true
        default: "vendor/trinket-perf_defconfig"

env:
  KBUILD_BUILD_USER: "@ravindu644"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install dependencies (non-interactive)
        run: |
          sudo apt-get update
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y \
            device-tree-compiler lz4 xz-utils zlib1g-dev openjdk-17-jdk \
            gcc g++ python3 python-is-python3 p7zip-full \
            android-sdk-libsparse-utils erofs-utils flex bison gperf \
            build-essential zip curl libc6-dev libncurses-dev libx11-dev \
            libreadline-dev libgl1 libgl1-mesa-dev make bc grep tofrodos \
            python3-markdown libxml2-utils xsltproc libtinfo6 cpio kmod \
            openssl libelf-dev pahole libssl-dev libarchive-tools zstd \
            libyaml-dev
          # legacy libtinfo5 some prebuilt clang releases still link against
          # NOTE: this .deb is from an old Ubuntu security pool and may 404
          # on newer runner images — verify this step still succeeds
          wget -q http://security.ubuntu.com/ubuntu/pool/universe/n/ncurses/libtinfo5_6.3-2ubuntu0.1_amd64.deb \
            && sudo dpkg -i libtinfo5_6.3-2ubuntu0.1_amd64.deb || true

      - name: Cache toolchains
        id: cache-toolchains
        uses: actions/cache@v4
        with:
          path: ~/toolchains
          key: toolchains-clang-r383902b-gcc-14.2.rel1

      - name: Download toolchains
        if: steps.cache-toolchains.outputs.cache-hit != 'true'
        run: |
          mkdir -p ~/toolchains/clang-r383902b
          curl -L -o /tmp/clang.tar.gz \
            "https://android.googlesource.com/platform/prebuilts/clang/host/linux-x86/+archive/0e9e7035bf8ad42437c6156e5950eab13655b26c/clang-r383902b.tar.gz"
          tar -xf /tmp/clang.tar.gz -C ~/toolchains/clang-r383902b
          rm /tmp/clang.tar.gz

          mkdir -p ~/toolchains/gcc
          curl -L -o /tmp/gcc.tar.xz \
            "https://developer.arm.com/-/media/Files/downloads/gnu/14.2.rel1/binrel/arm-gnu-toolchain-14.2.rel1-x86_64-aarch64-none-linux-gnu.tar.xz"
          tar -xf /tmp/gcc.tar.xz -C ~/toolchains/gcc
          rm /tmp/gcc.tar.xz

      - name: Build kernel
        run: |
          set -e
          export ARCH=arm64
          export KERNEL_ROOT="$(pwd)"
          export PATH="${HOME}/toolchains/clang-r383902b/bin:${PATH}"
          export LD_LIBRARY_PATH="${HOME}/toolchains/clang-r383902b/lib:${HOME}/toolchains/clang-r383902b/lib64:${LD_LIBRARY_PATH}"
          export BUILD_CROSS_COMPILE="${HOME}/toolchains/gcc/arm-gnu-toolchain-14.2.rel1-x86_64-aarch64-none-linux-gnu/bin/aarch64-none-linux-gnu-"
          export BUILD_CC="${HOME}/toolchains/clang-r383902b/bin/clang"

          BUILD_OPTIONS=(
            HOSTLDLIBS="-lyaml"
            -C "${KERNEL_ROOT}"
            O="${KERNEL_ROOT}/out"
            -j"$(nproc)"
            ARCH=arm64
            CROSS_COMPILE="${BUILD_CROSS_COMPILE}"
            CC="${BUILD_CC}"
            CLANG_TRIPLE=aarch64-linux-gnu-
          )

          mkdir -p "${KERNEL_ROOT}/out" "${KERNEL_ROOT}/build"

          # apply defconfig, then resolve any new symbols with defaults
          # (this replaces the interactive `make menuconfig` step —
          # menuconfig cannot run in a CI job with no attached terminal)
          make "${BUILD_OPTIONS[@]}" "${{ github.event.inputs.defconfig }}"
          make "${BUILD_OPTIONS[@]}" olddefconfig

          make "${BUILD_OPTIONS[@]}" Image

          cp "${KERNEL_ROOT}/out/arch/arm64/boot/Image" "${KERNEL_ROOT}/build/"

      - name: Upload Image artifact
        uses: actions/upload-artifact@v4
        with:
          name: Image-${{ github.event.inputs.defconfig }}
          path: build/Image
